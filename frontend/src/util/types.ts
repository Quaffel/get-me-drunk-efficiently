import React from 'react';

/* export type WritableRefObj<T> = Mutable<React.RefObject<T>>
export type WritableRefCallback<T> = Mutable<React.RefCallback<T>>;

type Mutable<T> = {
    -readonly [P in keyof T]: T[P];
}; */